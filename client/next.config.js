/** @type {import('next').NextConfig} */
const nextConfig = {
  // reactStrictMode: true,
  // swcMinify: true,
  //   webpack: function (config, options) {
  //       options.webpack.poll = 300
  //       return config
  //   },

    assetPrefix: "."
}

module.exports = nextConfig
